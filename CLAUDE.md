# physics-study — Teaching Workspace ฟิสิกส์ทฤษฎี

> **Context file สำหรับ Claude Code** — โหลดอัตโนมัติทุก session ในโฟลเดอร์นี้

## นี่คืออะไร

**Teaching workspace** ไม่ใช่โปรเจกต์ซอฟต์แวร์ — ของที่ผลิตออกมาคือ *บทเรียน*
ไม่ใช่ฟีเจอร์ · ขับด้วยสกิล **`/teach`** (`.claude/skills/teach/`) ซึ่งแบต้อง
พิมพ์เรียกเอง (`disable-model-invocation: true` — Claude เรียกให้ไม่ได้)

- **เจ้าของ:** แบฟี — ครูคณิตศาสตร์ ม.ปลาย · ติว สอวน. คอมพิวเตอร์ · กำลังจะเรียนต่อ **ป.เอก ฟิสิกส์ทฤษฎี**
- **แยกออกมาจาก `rpg-quiz` โดยตั้งใจ** — repo นั้นเป็นเว็บแอปคณิต มี `CLAUDE.md`
  4,000+ บรรทัดที่ไม่เกี่ยวกับการเรียนฟิสิกส์เลย · ปนกันแล้วทั้งคอนเท็กซ์และ
  git history จะสับสน

## โครงไฟล์ (สกิล `teach` เป็นคนกำหนด)

```
MISSION.md            เหตุผลที่อยากเรียน — ground ทุกบทเรียน (สกิลถามก่อนสอนบทแรก)
RESOURCES.md          แหล่งอ้างอิงที่เชื่อถือได้ + คุณภาพของแต่ละแหล่ง
NOTES.md              สไตล์การสอนที่แบชอบ · ข้อควรจำ
lessons/NNNN-*.html   บทเรียนทีละชิ้น — สั้น จบในหนึ่งนั่ง มีชัยชนะหนึ่งอย่าง
reference/*.html      ชีทสรุป/glossary — ของที่กลับมาเปิดซ้ำ (บทเรียนไม่ค่อยถูกเปิดซ้ำ)
learning-records/     บันทึกว่าเรียนอะไรไปแล้ว → ใช้คำนวณ zone of proximal development
assets/               stylesheet + quiz widget ที่ทุกบทเรียนใช้ร่วมกัน
```

## กฎของ workspace นี้

1. **ภาษาไทยเสมอ** — ศัพท์เทคนิคใช้อังกฤษตามเดิม (Lagrangian, gauge, tensor,
   eigenstate) เพราะแบต้องอ่านตำราและ paper ภาษาอังกฤษอยู่แล้ว การแปลศัพท์
   เป็นไทยจะทำให้จำคำที่ใช้จริงไม่ได้
2. **กระชับ ตรงประเด็น** — ไม่ต้องคำนำ ไม่ต้องสรุปซ้ำ
3. ⚠️ **ห้ามเชื่อความรู้ในหัวตัวเอง** (กฎของสกิล: *"Never trust your parametric
   knowledge"*) — ทุกข้ออ้างในบทเรียนต้องมี citation ไปแหล่งจริง · ฟิสิกส์ทฤษฎี
   เป็นสาขาที่ผิดแล้วดูเนียนที่สุด (เครื่องหมาย convention ต่างตำรา · factor ของ
   $2\pi$ · metric signature $(-+++)$ กับ $(+---)$)
4. **คณิตต้องคำนวณจริง ไม่ใช่พิมพ์ผลลัพธ์จากความจำ** — ใช้ `sympy` ตรวจการอนุพันธ์
   /อินทิเกรต/พีชคณิตเทนเซอร์ก่อนเขียนลงบทเรียนเสมอ · แบเป็นครูคณิต จับผิด
   พีชคณิตได้ทันที แต่นั่นไม่ใช่หน้าที่ของคนเรียน
5. **แบเป็นครูคณิต ไม่ใช่มือใหม่คณิต** — ข้ามการปูพื้นแคลคูลัส/พีชคณิตเชิงเส้น/
   ความน่าจะเป็นได้เลย · สิ่งที่ต้องปูคือ **ฟิสิกส์** และ **คณิตของฟิสิกส์**
   (variational calculus, differential geometry, group theory) ที่คณิต ม.ปลาย
   ไม่มี
6. **LaTeX ในบทเรียนใช้ KaTeX ผ่าน CDN** delimiter `$...$` / `$$...$$` (แบชินกับ
   ชุดนี้อยู่แล้วจาก rpg-quiz)
7. ⚠️⚠️ **เขียนกฎลงไปแล้วไม่มีข้อฝึกคู่กัน = ยังไม่ได้สอน** (วัดแล้ว 2026-09-15)
   — ไล่ข้อฝึกทั้งหมด **104 รายการ** ที่แบเคยทำ แล้วพบว่าปฏิบัติการสี่อย่างที่
   เขียนไว้ในเนื้อบทแต่**ไม่เคยมีข้อไหนบังคับให้ใช้เลย** (กระจาย $V$ · ตรวจสมมาตร ·
   คำนวณ $v^\mu v_\mu$ เป็นตัวเลข · นับด้วยปฏิสมมาตร) คือปฏิบัติการที่พลาด
   **ครบทั้งสี่ข้อพอดี** ส่วนข้อที่ผ่านมีแบบฝึกรองรับทุกข้อ — ตรงเป๊ะ ไม่มีข้อยกเว้น
   · ⇒ ก่อน publish ให้ไล่ดูว่า**ทุกสมการที่ตั้งเลขไว้มีข้อฝึกที่ใช้มันจริง** ·
   เนื้อหาที่ยังไม่มีข้อฝึกให้เขียนว่า "ยังไม่แตะโดยตั้งใจ" แทนการพูดทิ้งไว้
8. **สัญกรณ์ที่ไม่เคยถูกนิยาม คือกฎที่ไม่เคยถูกสอน** — $\eta$ ถูกใช้ใน 7 โจทย์
   แต่มีเพียง **1 โจทย์**ที่บอกว่ามันคืออะไร ⇒ แบติดตั้งแต่ขั้นอ่านโจทย์ ไม่ใช่ขั้นฟิสิกส์
   · สัญลักษณ์ใหม่ทุกตัวต้องมีประโยคที่บอกว่า "อ่านมันออกจากอะไร"
9. ⚠️⚠️ **“มีข้อฝึกคู่กัน” ยังไม่พอ — ต้องเป็นข้อที่ผู้เรียน *ผลิต* มันออกมา**
   (วัดแล้ว 2026-09-20) · นับอัตราส่วน **ให้ไว้ : ถามเอา** ของข้อเท็จจริงที่พลาด:
   $[\mathcal{L}] = M^4$ ถูก**ให้ไว้ 6 ครั้งใน 5 บท** และ**ถามเอา 0 ครั้ง** —
   ทุกข้อนับมิติที่เคยทำ ($\lambda\phi^4 \to 0$ · $g\phi^6 \to -2$) ใช้มันเป็น
   *วัตถุดิบ* และทำถูกหมด แต่พอถามตัวมันตรง ๆ ตอบ $0$ (= มิติของ $S$ ซึ่งเป็น
   ขั้นแรกของการหามัน) · ⇒ ก่อน publish ให้ถามว่า **“ข้อเท็จจริงนี้เคยเป็น
   *คำตอบ* ของข้อไหนหรือยัง”** ไม่ใช่แค่ “เคยโผล่ในข้อไหนหรือยัง”
   · และ **ห้ามเขียน “ตรวจแล้วว่า…” แล้วเอาไปถาม** — นั่นเป็นการรายงานว่าเราตรวจมา
     ไม่ใช่การสอนว่ามันมาจากไหน (เกิดจริงกับ $-\tfrac14F^2 = \tfrac12(E^2-B^2)$
     ⇒ ผู้เรียนตอบ "ไม่รู้" ซึ่งถูกต้องตามสิ่งที่บทให้ไป)
10. **ปฏิบัติการหลายพจน์ต้องแตกเป็นขั้น ห้ามรวบเป็นช่องเดียว** —
   $\sum_i(\partial L/\partial x_i)\delta x_i$ ที่ให้ตอบทั้งก้อนครั้งเดียว ได้คำตอบ
   ที่เป็น $\partial L/\partial x_3$ ตัวเดียว (อนุพันธ์ถูกหมด แต่ไม่ได้บวก) ·
   พอแตกเป็นขั้นละตัวแล้วค่อยบวก **ผ่าน 7/7** — สิ่งที่แก้คือรูปของข้อ ไม่ใช่คำอธิบาย

## Environment

```bash
pip install -r requirements.txt   # sympy numpy scipy matplotlib
python -c "import sympy; print(sympy.__version__)"
```

บทเรียนเป็นไฟล์ HTML ล้วน เปิดด้วยเบราว์เซอร์ได้ตรง ๆ ไม่ต้องมี server

## สั่งงานจาก iPad/มือถือ

`claude.ai/code` → เลือก repo `FeePhysics/physics-study` → พิมพ์ `/teach`
· session รันบนคลาวด์ แก้เสร็จเปิด PR ให้ · merge จากแอป GitHub ได้
· ⚠️ **cloud session เห็นเฉพาะของที่ push แล้ว** — ก่อนออกจากคอมให้ commit+push

## Assets ที่มีแล้ว — ใช้ซ้ำก่อนเขียนใหม่เสมอ

| ไฟล์ | ใช้ทำอะไร |
|---|---|
| `assets/lesson.css` | stylesheet ร่วมของทุกบท — Tufte-ish · dark mode · **print styles** (บทเรียนถูกปรินต์จริง) |
| `assets/quiz.js` | แบบฝึกที่ให้ feedback ทันที · แค่วาง markup ไม่ต้องเขียน JS เพิ่ม |
| `assets/_template.html` | โครงบทเรียน — ก๊อปไปเป็น `lessons/NNNN-*.html` แล้วเติมเนื้อ |
| `scripts/build_standalone.py` | แปลงบทเป็น**ไฟล์เดียวจบ** — ฝัง CSS+JS และแปลง LaTeX เป็น **MathML** ⇒ เปิดที่ไหนก็ได้โดยไม่ต้องมี CDN |

**ส่งบทเรียนให้แบต้องส่งฉบับ `build/` เสมอ** — ไฟล์ใน `lessons/` ลิงก์
`../assets/lesson.css` แบบ relative ⇒ ดาวน์โหลดไฟล์เดียวไปเปิดจะไม่มีสไตล์
และ quiz ไม่ทำงาน (เกิดจริงมาแล้ว) · `build/artifact-*.html` คือฉบับสำหรับ
publish เป็น Artifact (ไม่มีแท็ก html/head/body เพราะตัวห่อถูกใส่ให้ตอน publish)
· `build/` ไม่เข้า git — สร้างใหม่ทับทุกครั้ง **ห้ามแก้ไฟล์ในนั้นโดยตรง**

**สร้างของใหม่ที่บทถัดไปอาจใช้ซ้ำ → เขียนเป็น component ใน `assets/`** อย่า inline
ลงบทเรียนเดียว (กฎของสกิล: *"Reuse is the default, not the exception"*)

## ประตูตรวจ — รันก่อน commit ทุกครั้ง

```bash
python3 scripts/check_lessons.py      # อ่านไฟล์
python3 scripts/check_matcher.py      # ตัวตรวจคำตอบรับ/ไม่รับอะไรบ้าง
python3 scripts/check_build_fresh.py  # build/ ตรงกับ lessons/ ไหม
python3 scripts/check_layout.py       # เปิดเบราว์เซอร์จริง (pip install playwright ก่อน)
```

⚠️ **`check_build_fresh.py` จำเป็นเพราะตัวที่ publish คือ `build/` แต่อีกสองประตู
อ่าน `lessons/` ทั้งคู่** ⇒ `build/` ค้างเก่าได้โดยประตูผ่านฉลุย · เกิดจริงมาแล้ว:
รัน `build_standalone.py` **ลืมใส่ `--all`** มันพิมพ์ docstring แล้ว exit ซึ่งหน้าตา
เหมือนรันสำเร็จ และเกือบ publish ฉบับก่อนแก้

⚠️ **`check_layout.py` ต้องตรวจ `build/` ไม่ใช่ `lessons/`** — `lessons/` โหลด KaTeX
จาก CDN ซึ่งถูกบล็อกในคลาวด์ ⇒ ประตูวัดความกว้างของ **LaTeX ดิบ** ไม่ใช่สมการที่
เรนเดอร์แล้ว · ทั้งรายงานผิด (ตาราง `$...$` ยาวล้นทั้งที่ของจริงไม่ล้น) และ
**มองไม่เห็นการล้นจริง** — พอชี้ไป `build/` ก็เจอทันทีว่าบท 0013 กับ 0014 ที่
publish ไปแล้ว **ล้นจอ 375px จริง** (487px และ 390px) จากสมการ inline ในหัวข้อ
ที่ตัดบรรทัดไม่ได้ · แก้ที่ `lesson.css` ด้วย `math { max-width:100%; overflow-x:auto }`
(ทดลองสี่ทางเลือกบนเบราว์เซอร์จริงก่อนเลือก) · `check_build_fresh.py` การันตี
อยู่แล้วว่า `build/` ตรงกับ `lessons/`

⚠️ **ตัวตรวจคำตอบของ `quiz.js` ตัดวงเล็บทิ้งโดยไม่กระจาย** ⇒ `k*(x2-x1)` กับ
`k*x2-k*x1` เป็นคนละสตริง · คีย์ที่ใส่ `data-alt` ไม่ครบจะ**ปฏิเสธคำตอบที่ถูก** ·
`check_matcher.py` ทำข้อนี้เป็นประตูถาวรแล้ว — จำลอง `norm()`/`num()`/`judge()`
ใน Python แล้วยิงสองฝั่งกับทุก `data-answer` ในทุกบท: ฝั่งรับ (คีย์ · `data-alt` ·
เว้นวรรค · ขีดยูนิโคด · `+` นำหน้า · `**` แทน `^`) ต้องได้ `right` · ฝั่งปฏิเสธ
(เครื่องหมายกลับ · คลาดไปหนึ่ง) ต้องได้ `wrong` · **ตัวตรวจที่รับทุกอย่าง
คือตัวตรวจที่ไม่ได้ตรวจอะไร**

· **ความเข้าใจผิดเฉพาะบทใส่ใน `HAND`** — ตัวสร้างอัตโนมัติจับได้แค่สองแบบข้างบน
ส่วนค่าที่ผู้เรียนตอบผิดมาจริง ๆ ต้องเขียนมือ
· ฝั่งรับตก = **แก้ `quiz.js`** ห้ามแก้คีย์ให้ประตูผ่าน

ตรวจ: `$`/`$$` เป็นคู่ · ลิงก์ไม่ตาย · ทุกบท link stylesheet + มีกล่อง `.ask` ·
`data-answer` อยู่ในช่วง · quiz มี `.fb` · **ตัวเลือกยาวไม่ต่างกันเกิน 1.6 เท่า**
(ตัวที่ยาวกว่าเพื่อน = เฉลยที่มองเห็นได้โดยไม่ต้องคิด)

· **ห้ามมี `$` ใน `<svg>`** — KaTeX ไม่เดินเข้าไปเรนเดอร์ในนั้น จะขึ้น LaTeX ดิบ
โดยไม่มีอะไรฟ้อง (เกิดจริงในบทที่ 1 · ใช้ยูนิโคด α β θ − แทน)

`check_layout.py` เปิด Chromium จริง ตรวจการล้นแนวนอนที่ 375px/1200px · JS error ·
และคลิก quiz ทุกอันดูว่าทำงานถูก — **ตารางกว้างต้องเลื่อนในกล่องตัวเอง ไม่ใช่ดัน
ทั้งหน้า** (ตาราง 6 คอลัมน์กว้าง 463px บนจอ 375px เกิดจริงในบทที่ 1 · แก้ที่
`lesson.css` แล้วด้วย `@media (max-width:640px){table{display:block;overflow-x:auto}}`)

ซ้อมทุบแล้วยิงครบทุกเคส และไม่ยิงตอนไฟล์ปกติ

## ⚠️ ข้อจำกัดของ Claude Code บนคลาวด์ (วัดแล้ว 2026-09-06)

**egress proxy บล็อกแทบทุกโดเมน** — CDN (`cdn.jsdelivr.net`, `cdnjs.cloudflare.com`)
และแหล่งอ้างอิงทั้งหมดที่ลองแล้ว (`wikipedia.org` · `arxiv.org` · `ocw.mit.edu` ·
`feynmanlectures.caltech.edu` · `theoreticalminimum.com`) ⇒ **ในคลาวด์ทำได้แค่
ค้นหา อ่านหน้าเว็บไม่ได้ และสมการไม่เรนเดอร์**

⇒ **บทเรียนต้องยืนบนเนื้อหาที่พิสูจน์เองได้** (คำนวณด้วย sympy แล้วตรวจซ้ำอีกวิธี)
ไม่ใช่บนการอ้างข้อความที่ยืนยันไม่ได้ · เก็บสคริปต์พิสูจน์ไว้ที่ `scripts/verify/`
· ฉบับ `build/` แปลงสมการเป็น MathML ⇒ **อ่านได้แม้ไม่มี CDN**

⇒ กฎบ้าน "ตรวจบนเบราว์เซอร์จริง KaTeX error 0" ทำในคลาวด์ไม่ได้ · ที่ทำได้คือ
`check_lessons.py` แล้ว **ให้แบเปิดไฟล์บนเครื่องตัวเองยืนยันอีกชั้น** ·
ห้ามรายงานว่า "ตรวจแล้วบนเบราว์เซอร์จริง" ถ้าตรวจจากคลาวด์

ทดสอบ `quiz.js` ในคลาวด์ยังทำได้ (ไม่พึ่ง CDN) — Chromium ติดตั้งไว้แล้วที่
`/opt/pw-browsers/chromium` ใช้ผ่าน `pip install playwright` (อย่ารัน `playwright install`)

เว็บที่บล็อกด้วย: `goodtheorist.science`, `webspace.science.uu.nl` (ดู `RESOURCES.md`)
